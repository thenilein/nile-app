import React from "react";

const App = () => {
    return (
        <div>
            <h1>React + Rspack + TypeScript</h1>
            <p>Basic setup is working 🚀</p>
        </div>
    );
};

export default App;